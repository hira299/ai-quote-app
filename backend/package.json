{
  "name": "quote-api",
  "version": "1.0.0",
  "description": "Quote API using Serverless Framework",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "serverless": "^3.38.0",
    "serverless-offline": "^11.1.3",
    "serverless-python-requirements": "^6.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
